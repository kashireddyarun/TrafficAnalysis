# Streamlit Cloud Compatible Requirements
# Core Streamlit and web components
streamlit>=1.28.0
plotly>=5.17.0

# Data Processing and Analysis
pandas>=2.0.0
numpy>=1.24.0

# Basic dependencies that work in cloud
requests>=2.31.0
pyyaml>=6.0

# Optional: Only include if actually needed and working in cloud
# opencv-python-headless>=4.8.0  # Use headless version for cloud
# ultralytics>=8.0.0  # May need system dependencies
# torch>=2.0.0  # Large package, only include if needed

# Utilities
tqdm>=4.65.0
